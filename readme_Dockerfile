### Fastest to spin up the container, with Dockerfile
# build images
$ docker build -t frontend .
# run the image as container
$ docker run -d --name=frontend -p=5000:8080 frontend:latest

# stop the container 
$ docker stop frontend
# remove the container
$ docker rm frontend
# remove the images
$ docker rmi frontend:latest


# To csee any docker object on your system:
$ docker system df
